#include <iostream>
using namespace std;
#include <bits/stdc++.h>

int main()
{
  list<int> l;
  l.push_back(10);
  l.push_back(20);
  l.push_back(30);
  l.push_back(40);
  l.push_back(50);
  // l.pop_back();
  // l.pop_front();

   

  // for (auto it = l.begin(); it != l.end(); it++)
  // {
  //   cout << *it << " " << endl;
  // }


  
  // for (auto it = l.rbegin(); it != l.rend(); it++)
  // {
  //   cout << *it << " " << endl;
  // }

  // cout<<l.front()<<" "<<l.back()<<" "<<l.size()<<endl;
}
